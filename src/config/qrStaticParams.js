// Update this list with the QR detail paths that should be pre-rendered when
// running `next export`. Each entry should include the `requestId`, `itemId`,
// and `token` segments exactly as they appear in the QR URLs.
//
// Example:
// export default [
//   { requestId: "123", itemId: "456", token: "abc" },
//   { requestId: "789", itemId: "012", token: "def" },
// ];

const qrStaticParams = [];

export default qrStaticParams;
